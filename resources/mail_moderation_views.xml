<?xml version="1.0" encoding="utf-8"?>
<hexya>
    <data>
	<view id="mail_moderation_view_tree" model="MailModeration" >
                <tree string="Moderation Lists" editable="bottom">
                    <field name="channel_id"/>
                    <field name="email"/>
                    <field name="status"/>
                </tree>
            </view>
	<view id="mail_moderation_view_search" model="MailModeration" >
                <search string="Search Moderation List">
                    <field name="channel_id"/>
                    <field name="email"/>
                    <field name="status"/>
                    <filter string="Is Banned"
                            name="status_ban" help="Banned Emails"
                            domain="[('status', '=', 'ban')]"/>
                    <separator/>
                    <filter string="Is Allowed"
                            name="status_allow" help="Allowed Emails"
                            domain="[('status', '=', 'allow')]"/>
                </search>
            </view>
	<action id="mail_moderation_action" type="ir.actions.act_window" name="Moderation" model="MailModeration" view_mode="tree,form" >
    </data>
</hexya>
